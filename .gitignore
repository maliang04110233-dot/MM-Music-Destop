node_modules/
dist/
out/
*.log
.DS_Store
Thumbs.db

# Electron
*.asar
*.asar.bak

# Environment
.env
.env.local

# IDE
.vscode/
.idea/

# OS
Desktop.ini

# Build output
release/
*.blockmap
# electron-builder 的发布元数据。**不要写 `*.yml`** —— 那会连
# .github/workflows/ci.yml 一起忽略（GitHub 的 CI 配置被静默丢掉，
# 本地完全看不出来，直到发现 Actions 一直没跑）。
# 只在 release/ 下按需忽略：见下方 `release/*.yml`。
release/*.yml
build-log.txt
build-log-full.txt

# 二进制/播放器依赖走 GitHub Releases 分发，不进仓库
*.zip
*.exe
*.7z
*.tar.gz

# 本机临时路径命名的历史误提交目录（防再犯）
Users*AppData*
C:*Users*

# 本地验证台与重构前备份（不入仓库）
.preview/
.backup/
.git-rescue/

# ── 误入仓库的构建日志 ─────────────────────────────────
# 这两条是 Windows 下 `some-cmd > C:/Users/.../x.js` 被 shell 吞掉反斜杠后
# 在仓库根目录**字面创建**的文件（文件名成为 `C:Users59443AppData...`）。
# 内容是别的机器上 asar 解包的报错输出，无任何代码引用。
# 用通配形式兜住同类问题，避免再靠人工发现。
C:Users*
npm-debug.log*
