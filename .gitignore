node_modules/
dist/
out/
*.log
.DS_Store
Thumbs.db

# Electron
*.asar
*.asar.bak

# Environment
.env
.env.local

# IDE
.vscode/
.idea/

# OS
Desktop.ini

# Build output
release/
*.blockmap
# electron-builder 的发布元数据。**不要写 `*.yml`** —— 那会连
# .github/workflows/ci.yml 一起忽略（GitHub 的 CI 配置被静默丢掉，
# 本地完全看不出来，直到发现 Actions 一直没跑）。
# 只在 release/ 下按需忽略：见下方 `release/*.yml`。
release/*.yml
build-log.txt
build-log-full.txt

# 二进制/播放器依赖走 GitHub Releases 分发，不进仓库
*.zip
*.exe
*.7z
*.tar.gz

# ── 误入仓库的本机路径文件（防再犯）────────────────────
# 来源：Windows 下 `some-cmd > C:\Users\...\x.js` 的路径分隔符被 shell 吞掉，
# 于是在仓库根目录**字面创建**了名为 `C⟨U+F03A⟩Users59443AppData...` 的文件
# （U+F03A 是 Windows 对非法字符 `:` 的读回映射）。
# 内容是别的机器上 asar 解包的报错输出，全仓无任何引用，已取消跟踪。
#
# ⚠️ 写法必须是 `C*Users*`，**不能写 `C:*Users*`**：
#    gitignore 里含 `:` 的模式永远匹配不上真实文件名（`:` 被当作锚点处理），
#    写出来看着合理、实际是条死规则。这一点已用 `git check-ignore --no-index`
#    逐个验证过 —— 是「守卫静默失效」的又一个变体。
C*Users*
Users*AppData*
npm-debug.log*

# 本地验证台与重构前备份（不入仓库）
.preview/
.backup/
.git-rescue/




.workbuddy-ai-test/
release-out/

# 一次性探针/诊断输出（不入仓库）
spark-output/

# 根目录本机启动 bat 与一次性探针脚本（取消跟踪，保留本地副本）
/MusicDownloader.bat
/dev.bat
/start.bat
/test-lib-probe.js
/test-local-cover-lrc.js
/test-local-meta-fallback.js
/test-m4a-real.js
/test-online-lrc.js
/test_console.py
